services:
  mlhr:
    container_name: mlhr-api-new
    build:
      context: .
      dockerfile: MLHR/Dockerfile
    ports:
      - "8086:8086"  # HTTP API
    environment:
      - ASPNETCORE_URLS=http://+:8086/
    networks:
      - demo

networks:
  demo:
    driver: bridge
